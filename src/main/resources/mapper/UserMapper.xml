<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.helppet.mapper.UserMapper">
    <!--아이디 중복확인-->
    <select id="idCheck" resultType="_int">
        SELECT COUNT(USER_ID) FROM TBL_USER WHERE USER_ID = #{userId}
    </select>

    <!--회원 등록-->
    <insert id="join"> <!--insert, update, delete의 리턴값은 자동으로 int가 된다-->
        INSERT INTO TBL_USER
        (
            USER_NUM, USER_ID, USER_NAME, USER_PW, USER_NICKNAME, USER_EMAIL, USER_PHONENUM, USER_ANIMALTYPE
        )
        VALUES(#{userNum}, #{userId}, #{userName}, #{userPw}, #{userNickName}, #{userEmail}, #{userPhoneNum}, #{userAnimalType})
    </insert>

</mapper>